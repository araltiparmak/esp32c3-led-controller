Create a new release and push OTA update to all ESP32 devices.

1. Find the latest git tag and calculate the next patch version
2. If there are uncommitted changes, commit them first
3. Create and push the new tag
4. Show the GitHub Actions URL to monitor the build
